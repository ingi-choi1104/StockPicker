rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 이벤트 컬렉션: 누구나 읽기 가능, 쓰기는 불가 (Functions가 Admin SDK로 씀)
    match /events/{eventId} {
      allow read: if true;
      allow write: if false;
    }
  }
}
